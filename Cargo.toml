[package]
name = "ml_lib"
version = "0.1.0"
edition = "2024"

[lib]
name = "ml_lib"
path = "src/lib.rs"

[dependencies]
rand = "0.10.0-rc.6"
rand_distr = "0.6.0-rc.0"
indicatif = "0.18.3"
serde = { version = "1.0.228", features = ["derive"] }
bitcode = { version = "0.6.9", features = ["serde"]}

[features]
parallel_ops = ["rayon"]

[dependencies.rayon]
version = "1.11.0"
optional = true
